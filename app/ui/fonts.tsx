import { DM_Sans, Lusitana } from 'next/font/google';

export const dmSans = DM_Sans({
  subsets: ['latin'],
});

export const lusitana = Lusitana({
  weight: ['400', '700'],
  subsets: ['latin'],
});
